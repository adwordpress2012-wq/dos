# ── BookOS Marketing Site — Environment Variables ──────────────────────────────
# Copy to .env (staging) or .env.production and fill in values.

# API backend (staging uses staging URL, production uses production URL)
VITE_API_BASE_URL=https://staging.directiveos.com.au/api

# BookOS demo call number shown in the "Try Sarah Now" CTA
VITE_BOOKOS_DEMO_NUMBER=0285504038
VITE_BOOKOS_DEMO_DISPLAY=02 5850 4038
